import 'package:flutter_test/flutter_test.dart';
import 'package:hello/classes/drawer_tile.dart';
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';



void main(){
 testWidgets('Testing Drawer_Tile widget', (tester) async {
    // await tester.pumpWidget(MaterialApp(home: Drawer_Tile(// Lacking arguments),));

    // //Trying to find Padding widget
    // Finder padding = find.byType(Padding);
    // expect(padding, findsOneWidget);
  });
}